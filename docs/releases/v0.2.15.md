## 监控优化

- 每轮监控最多并发检测 5 个账号，减少慢账号对整轮检测的阻塞。
- 单个监控请求超过 40 秒自动记为失败，并写入监控记录。
- 所有账号完成检测后，再按成功账号的倍率和平均耗时选择 candidate，原有调度规则保持不变。

## 更新体验

- 应用启动进入主界面 5 秒后自动检查更新。
- 应用持续运行时每 6 小时自动检查更新。
- 自动检查无更新或网络异常时保持静默，手动检查更新入口继续保留。
